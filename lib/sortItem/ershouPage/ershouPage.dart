import 'package:flutter/material.dart';

class ErshouPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('二手房'),
        elevation: 0.0,
      ),
    );
  }
}
